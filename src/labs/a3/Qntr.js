import { useState } from "react";

function Qntr({qwe}) {
  const [qwe, wer] = useState(wer);
  return (
    <div>
      <h2>{qwe}</h2>
      <button onClick={() => asd(wer + 20)}>ZXC</button>
      <button onClick={() => asd(wer - 30)}>CXZ</button>
    </div>
  );
}

export default Qntr;